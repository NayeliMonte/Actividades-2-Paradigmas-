#include <stdio.h>

int main() {
    int numero;
    int r;
 
    printf("Programa para calcular la mitad de un numero más su quinta parten"); 
    printf("Ingrese el número: ");
    scanf("%d", &numero); 
    
    r = (numero/2) + (numero/5);
 
    printf("El resultado es: %d", r); 
 
    return 0;
}